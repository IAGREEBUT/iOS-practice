import UIKit

var greeting = "Hello, playground"

/*
 코드의 범위
 
 Global {} 외부, 선언 순서에 관계없이 접근가능
 
 Local {} 내부, 로컬스코프 내에서는 상위,글로벌 스코프에 접근가능
 
 범위규칙
 1. 동일한 스코프에 접근할 수 있음
 2. 글로벌 스코프에ㅓㅅ는 선언 순서 상관없이 접근 가능
 3. 로컬 스코프에서 상위 스코프나 글로벌 스코프에 접근 가능.
 4. 글로벌 스코프가 앙니라면 이미 선언되어 있는 요소에만 접근 가능
 5. 상위 스코프는 하위 스코프에 접근 할 수 없음
 6. 서로 다른 스코프에 동일한 이름이 존재하면 계층구조 상 가장 가까운 스코프의 이름을 사용
 7. 글로벌 스코프가 아닌 다른 모든 스코프는 시작과 끝이 명확해야함 
 */


/*
 
 */
